import { render } from 'react-dom';
import { App } from './App';

// primeiro parâmetro é o que você quer exibir na tela
// segundo parâmetro é dentro de qual elemento vc quer exibir essa informação?
render(<App/ >, document.getElementById("root"))